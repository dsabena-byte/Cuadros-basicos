# Por ahora el dashboard sólo lee los JSON estáticos de /public/data/.
# Cuando se conecte el flujo de Power Automate (SharePoint → ventas.json),
# acá vamos a agregar el secret compartido para el webhook de refresh.
# REFRESH_SECRET=
